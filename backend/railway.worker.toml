[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.worker"

[deploy]
startCommand = "celery -A app.core.celery_app worker -Q campaigns,analytics --loglevel=info --concurrency=4"
restartPolicyType = "ON_FAILURE"
